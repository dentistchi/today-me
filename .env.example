# 이메일 발송 설정
# .env 파일로 저장하고 환경 변수로 로드하세요

# SMTP 서버 설정 (Gmail 예제)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail 계정 정보
# 주의: 일반 비밀번호가 아닌 "앱 비밀번호"를 사용해야 합니다!
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# 발신자 이메일
FROM_EMAIL=your-email@gmail.com

# ============================================================================
# Gmail 앱 비밀번호 생성 방법
# ============================================================================
# 1. Google 계정 > 보안 > 2단계 인증 활성화
# 2. https://myaccount.google.com/apppasswords 접속
# 3. "앱 선택": 메일, "기기 선택": 기타 (사용자 지정)
# 4. 생성된 16자리 비밀번호 복사 (공백 제거)
# 5. 위의 SMTP_PASSWORD에 입력

# ============================================================================
# 다른 이메일 서비스 설정
# ============================================================================

# Naver Mail (네이버 메일)
# SMTP_HOST=smtp.naver.com
# SMTP_PORT=587
# SMTP_USER=your-id@naver.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-id@naver.com

# Daum Mail (다음 메일)
# SMTP_HOST=smtp.daum.net
# SMTP_PORT=465
# SMTP_USER=your-id@daum.net
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-id@daum.net

# Outlook / Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-email@outlook.com

# Office 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-email@yourdomain.com

# ============================================================================
# 환경 변수 로드 방법
# ============================================================================

# 방법 1: 터미널에서 직접 설정
# export SMTP_HOST=smtp.gmail.com
# export SMTP_PORT=587
# export SMTP_USER=your-email@gmail.com
# export SMTP_PASSWORD=your-app-password
# export FROM_EMAIL=your-email@gmail.com

# 방법 2: .env 파일 사용 (python-dotenv)
# pip install python-dotenv
# 
# Python 코드에서:
# from dotenv import load_dotenv
# load_dotenv()

# 방법 3: 시스템 환경 변수 설정
# Linux/Mac: ~/.bashrc 또는 ~/.zshrc 에 추가
# Windows: 시스템 속성 > 환경 변수

# ============================================================================
# 보안 주의사항
# ============================================================================
# ⚠️  이 파일을 Git에 커밋하지 마세요!
# ⚠️  .gitignore에 .env를 추가하세요
# ⚠️  프로덕션 환경에서는 AWS Secrets Manager, Azure Key Vault 등 사용
