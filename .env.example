# SMTP 이메일 설정
# Gmail 사용 예시:
# 1. Google 계정 > 보안 > 2단계 인증 활성화
# 2. 앱 비밀번호 생성 (https://myaccount.google.com/apppasswords)
# 3. 생성된 16자리 비밀번호를 SMTP_PASSWORD에 입력

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
FROM_EMAIL=noreply@selfesteem.com
FROM_NAME=자존감 연구팀

# 개발자/관리자 이메일 (알림 수신용)
# 사용자가 테스트 완료 시 개발자에게 알림 이메일 발송
# 24시간 후 사용자가 받을 최종 리포트를 미리 확인 가능
ADMIN_EMAIL=developer@example.com

# 이메일 전송 활성화 (true/false)
# false로 설정하면 테스트 모드 (로그만 출력)
ENABLE_EMAIL=false
