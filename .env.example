# ============================================================================
# 이메일 발송 설정 (.env 파일로 저장하세요)
# ============================================================================
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요:
#   cp .env.example .env
# 
# 그 다음 아래 설정을 실제 값으로 수정하세요.

# ============================================================================
# SMTP 서버 설정
# ============================================================================

# Gmail 사용 (권장)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP 인증 정보
# ⚠️  중요: Gmail은 "앱 비밀번호"를 사용해야 합니다 (일반 비밀번호 X)
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# 발신자 정보
FROM_EMAIL=your-email@gmail.com
FROM_NAME=자기자비 여정

# 이메일 발송 활성화 (true로 설정하면 실제 발송, false면 테스트 모드)
ENABLE_EMAIL=true

# ============================================================================
# Gmail 앱 비밀번호 생성 방법 (필수!)
# ============================================================================
# 1. Google 계정 설정
#    - https://myaccount.google.com/security 접속
#    - "2단계 인증" 활성화 (필수!)
#
# 2. 앱 비밀번호 생성
#    - https://myaccount.google.com/apppasswords 접속
#    - "앱 선택": 메일
#    - "기기 선택": 기타 (사용자 지정 이름 입력)
#    - 생성된 16자리 비밀번호를 위의 SMTP_PASSWORD에 입력
#    - 예: abcd efgh ijkl mnop (공백 포함 또는 제거 모두 가능)
#
# 3. .env 파일 저장 후 서버 재시작

# ============================================================================
# 다른 이메일 서비스 설정 예제
# ============================================================================

# Naver Mail (네이버 메일)
# SMTP_HOST=smtp.naver.com
# SMTP_PORT=587
# SMTP_USER=your-id@naver.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-id@naver.com

# Daum Mail (다음 메일)
# SMTP_HOST=smtp.daum.net
# SMTP_PORT=465
# SMTP_USER=your-id@daum.net
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-id@daum.net

# Outlook / Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-email@outlook.com

# Office 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASSWORD=your-password
# FROM_EMAIL=your-email@yourdomain.com

# ============================================================================
# 환경 변수 설정 방법
# ============================================================================

# 방법 1: .env 파일 사용 (권장)
# cp .env.example .env
# 위 파일을 편집하여 실제 값으로 변경
# 서버가 자동으로 .env 파일을 로드합니다

# 방법 2: 터미널에서 직접 설정
# export SMTP_HOST=smtp.gmail.com
# export SMTP_PORT=587
# export SMTP_USER=your-email@gmail.com
# export SMTP_PASSWORD=your-app-password
# export FROM_EMAIL=your-email@gmail.com
# export ENABLE_EMAIL=true

# 방법 3: 시스템 환경 변수 설정
# Linux/Mac: ~/.bashrc 또는 ~/.zshrc 에 추가
# Windows: 시스템 속성 > 환경 변수

# ============================================================================
# 테스트 방법
# ============================================================================

# 1. SMTP 설정이 올바른지 테스트
# python real_email_sender.py test

# 2. 테스트 이메일 발송
# python send_user_emails.py test

# 3. API 서버 시작
# python api.py
# 그 다음 POST /api/assess 엔드포인트로 테스트 요청

# ============================================================================
# 보안 주의사항
# ============================================================================
# ⚠️  이 파일(.env)을 Git에 커밋하지 마세요!
# ⚠️  .gitignore에 .env가 포함되어 있는지 확인하세요
# ⚠️  프로덕션 환경에서는 AWS Secrets Manager, Azure Key Vault 등 사용 권장
# ⚠️  앱 비밀번호는 주기적으로 변경하세요 (3개월 권장)
